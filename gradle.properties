# Gradle Properties
org.gradle.jvmargs = -Xmx1G
org.gradle.parallel = true

# Mod Properties
version = 1.0.0
maven_group = ho.artisan
archives_base_name = fireframe

tags_binder_version = 0.2

# Dependencies are managed at gradle/libs.versions.toml
